namespace AndreGoepel.Design.Blazor.Demo.Resources;

/// <summary>
/// Marker type for the demo app's own UI strings — the generic argument of
/// <c>IStringLocalizer&lt;Strings&gt;</c>. Kept separate from the library's
/// <c>DesignStrings</c>: an app translates its own content independently of the
/// design system it consumes, and each app typically has its own resource layout.
/// </summary>
public sealed class Strings;
